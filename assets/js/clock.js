// SET/RECALL button:
	// HOLD to set date & time. (A whole other series of functions.)
	// 24-HR mode:
		/*
		if ( ( 24-HR = true ) && ( ampm = pm ) && ( time >= 1:00 PM ) {
			time = time + 12:00;
		}
		else if ( ( 24-HR = true ) && (ampm = am ) && time <= 1:00 AM ) {
			time = time - 12:00;
		}
		*/

// MODE button:
	// Goto: CHRONO.

// START/SPLIT button:
	// Press to view Time 1 / Time 2.
	// HOLD until it toggles permanently to either Time 1 / Time 2.

// STOP/RESET button:
	// N/A.

// INDIGLO button:
	// Link to indiglo.js.