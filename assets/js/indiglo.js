// If nightMode == false:
	// Lights for X seconds.
	// HOLD UNTIL BEEPS: nightMode == true. (Stays lit for X seconds every time any button is pressed.)
// If nightMode == true:
	// Lights for X seconds.
	// HOLD UNTIL BEEPS: nightMode == false. (Lights up only when Indiglo button is pressed.)