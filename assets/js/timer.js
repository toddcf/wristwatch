// SET/RECALL button:
	// HOLD TO SET.
		// MODE button: Next field.
		// START/SPLIT button: ++
		// STOP/RESET button: --

// MODE button:
	// If < X seconds: Goto: ALARM.
	// If > X seconds: Goto: CLOCK.

// START/SPLIT button:
	// If timer = Stopped: Start timer countdown.
	// If timer = Active: ??

// STOP/RESET button:
	// If timer = Active: Stop timer.
	// If timer = Stopped: Reset.

// INDIGLO button:
	// Link to indiglo.js.